# Cursivis website

The production landing page for [cursiviss.vercel.app](https://cursiviss.vercel.app).

```powershell
npm ci
npm test
```

`npm test` creates the static production export and validates the download,
installation, feature, SmartScreen, and privacy content contract. Vercel hosts
only this website; Windows installers are published through GitHub Releases.
