<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Cursivis.Windows.App.Controls.SaveStateBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Style="{StaticResource QuietSurfaceBorderStyle}">
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                x:Name="StateTextBlock"
                VerticalAlignment="Center"
                Style="{StaticResource BodySecondaryTextStyle}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button
                    x:Name="DiscardButton"
                    x:Uid="DiscardChangesButton"
                    AutomationProperties.AutomationId="DiscardSectionChanges"
                    Click="OnDiscardClicked" />
                <Button
                    x:Name="SaveButton"
                    x:Uid="SaveChangesButton"
                    AutomationProperties.AutomationId="SaveSectionChanges"
                    Click="OnSaveClicked"
                    Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
